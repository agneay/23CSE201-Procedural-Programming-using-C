#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <stdbool.h>

int revNum(int num);
int main()
{
    int num;
    printf("Enter any integer: ");
    scanf("%d", &num);
    int revNum = reverseDigit(num);
    return 0;
}

int revNum(int num)
{
    int revNum = num;
    while(revNum!=0){
        revNum 
    }
    return;
}